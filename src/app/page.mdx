import { Guides } from '@/components/Guides'
import { Resources } from '@/components/Resources'
import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  title: 'API Documentation',
  description:
    'Learn everything there is to know about the Protocol API and integrate Protocol into your product.',
}

export const sections = [
  { title: 'Guides', id: 'guides' },
  { title: 'Resources', id: 'resources' },
]

<HeroPattern />

# Watchdog Documentation

Use the Watchdog API to provide the user with eBay scraping resources, with the world class advanced scraping system that we provide. {{ className: 'lead' }}

<div className="mb-16 mt-6 flex gap-3">
  <Button href="/authentication" arrow="right" children="Quickstart" />
</div>

## Getting started {{ anchor: false }}

To get started, create a new account on [watchdog.com](https://watchdog.com), then read about how to make requests for the resources you need to access using our HTTP APIs. {{ className: 'lead' }}

<Guides />

<Resources />
